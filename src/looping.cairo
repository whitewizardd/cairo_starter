

fn main() {

    // let mut x = 0_u8;

    // loop {
    //     if (x == 10) {
    //         break;
    //     }
    //     println!("the numnber is {x}");
    //     x += 1;
    // }

    println!("running a while loop here ::: ");

    let mut y = 0;

    while y != 10 {
        println!("number is {}", y * 2);
        y +=1;
    }
}